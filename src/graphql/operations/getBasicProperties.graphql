query getBasicProperties{
    applicationProperties {
        uiDebugMode
    }
    userAuthorities {
        roles {
            id
            name
            code
        }
        rights {
            name
        }
    }
}